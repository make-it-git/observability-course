apiVersion: v1
kind: Service
metadata:
  name: my-service
  labels:
    app: my-go-app
spec:
  selector:
    app: my-go-app
  ports:
  - port: 8080
    targetPort: 8080
    name: web
  type: ClusterIP
